# Githubification

### The method by which any repo can execute using GitHub as Infrastructure

Either the repo is:

- the source to an AI Agent that can use GitHub as AI Infrastructure
- the source to any software can be augmented with an AI Agent that uses GitHub as AI Infrastructure

